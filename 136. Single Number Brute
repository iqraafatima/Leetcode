class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int k;
        for(int i=0;i<nums.size();i++){
            int c=0;
            int num=nums[i];
            for(int j=0;j<nums.size();j++){
                if(nums[j]==num)
                c++;
            }
            if(c==1)
             k=nums[i];
        }
    return k;}
};
